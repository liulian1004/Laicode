public class Solution {
  public int sum(int[][] array) {
    // Write your solution here
    int sum = 0;
    for(int i = 0; i < array.length;i++){// 遍历row
      for(int j = 0; j < array[0].length;j++){//遍历column
        sum += array[i][j];
      }
    }
    return sum;
  }
}

public class Solution {
  public long power(int a, int b) {
    // Write your solution here
    //O(log n), O(log n)
    if(b == 0) {
      return 1;
    }
    long res = power(a,b/2);
    if(b % 2 == 0) {
      return res*res;
    }
    return res*res*a;
  }
}
